/**
 * Placeholder for the generated Supabase database types.
 *
 * Once the data-model.md migrations exist, regenerate this file with:
 *   pnpm supabase gen types typescript --local > types/database.ts
 *
 * Until then this empty shape lets the typed Supabase clients compile.
 */
export type Database = Record<string, never>;
